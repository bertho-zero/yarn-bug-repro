# yarn-bug-repro

```
git clone git@github.com:bertho-zero/yarn-bug-repro.git
cd yarn-bug-repro/packages/package-b
yarn start
```

result:

```
Usage Error: Couldn't find @storybook/react@npm:5.3.18 [0eea2] in the currently installed node_modules map - running an install might help

$ yarn run [--inspect] [--inspect-brk] <scriptName> ...
```
